version: "2"
sql:
  - engine: "sqlite"
    schema: "db/schema.sql"
    queries: "db/query.sql"
    gen:
      go:
        package: "gen"
        out: "db/gen"
        emit_json_tags: true
        emit_prepared_queries: false
        emit_interface: true
        emit_exact_table_names: false
