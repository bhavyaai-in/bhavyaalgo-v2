This project has a Graphify knowledge graph at graphify-out/graph.json.
Use the graphify MCP tools (query_graph, get_node, get_neighbors, shortest_path)
to answer codebase questions instead of grepping files directly.
Rebuild the graph with: cd /path/to/project && graphify update .
